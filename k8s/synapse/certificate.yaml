apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: synapse-cert
spec:
  secretName: synapse-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - matrix.kevingomez.fr
