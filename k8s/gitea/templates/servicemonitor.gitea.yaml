apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gitea
spec:
  selector:
    matchLabels:
      app: gitea

  namespaceSelector:
    matchNames:
      - gitea

  jobLabel: integrations/gitea
  endpoints:
    - port: http
      interval: 30s
      scheme: http
      authorization:
        type: Bearer
        credentials:
          name: metrics-tokensssssss
          key: password
          optional: false