apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: libretranslate
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`translate.home.lab`)
      middlewares:
        - name: forwardauth-authelia
      services:
        - kind: Service
          name: libretranslate-web
          port: 80