apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nanoflux-cert
spec:
  secretName: nanoflux-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - nanoflux.kevingomez.fr
