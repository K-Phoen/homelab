---
- name: Setup potato
  hosts: potato

  vars:
    docker_users: ["{{ homelab_user }}"]

  roles:
    - common
    - exit_node
    - k3s_node
    - potato

- name: Setup carrot
  hosts: carrot

  vars:
    docker_users: ["{{ homelab_user }}"]

  roles:
    - common
    - exit_node
    - k3s_control_plane
    - carrot

- name: Setup bean
  hosts: bean

  vars:
    docker_users: ["{{ homelab_user }}"]

  roles:
    - common
    - exit_node
    - k3s_node
    - bean
