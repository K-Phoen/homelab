---
- name: Setup nodes
  hosts: [potato, bean, carrot]

  roles:
    - common
    - k3s_control_plane
    - alloy_node
    - exit_node
    - dns_server
    - keepalived

- name: Setup backups
  hosts: [carrot]

  roles:
    - k3s_etcd_backups
